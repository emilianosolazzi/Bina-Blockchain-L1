# Deprecated Rust reference files

These files were removed from the active `l2-mining/rust` workspace because they are not part of the Cargo workspace runtime path.

Archived here:
- `Mining.rs`: stale legacy standalone miner/reference implementation
- `nist_pqc.rs`: standalone PQC experimentation/reference module superseded by `rust/temporal_gradient_core/src/pqc.rs`

Active runtime path:
- `rust/temporal_gradient_core`
- `rust/package`
